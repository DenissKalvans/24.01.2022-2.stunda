text = input("Ievadit virkne tekstu: ")
def Replace(tekst):
  if len(tekst)>1:
   sar = []
   for burts in tekst:
    sar.append(burts)
    sar.reverse()
    tekst = ""
   for elements in sar:
     tekst += elem
     tekst = tekst.upper()
     print (tekst)
   else:
     tekst = "Parak iss tekst"
   print(tekst)
replace(tekst)